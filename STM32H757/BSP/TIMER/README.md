# Timer BSP

Board-support boundary for periodic timing and timer peripherals. The BSP
provides DWT-backed microseconds and HAL-tick milliseconds; scheduling policy
belongs to System and timing consumers belong to higher layers.
