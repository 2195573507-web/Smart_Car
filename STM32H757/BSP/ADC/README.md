# ADC BSP

Board-support boundary for analog channels and conversion sampling. The
framework API is present, but the current IOC has no ADC instance or battery
channel, so operations return an explicit unsupported status.
