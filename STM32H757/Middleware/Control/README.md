# Control Middleware

Closed-loop control boundary for chassis and motor targets. It consumes
filtered sensor estimates and emits driver-level commands without depending on
Application implementation details.
