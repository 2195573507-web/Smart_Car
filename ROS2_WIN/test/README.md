# Workspace-level test fixtures

The package gtests live in `src/s3_ydlidar_bridge/test`. This directory is
reserved for captured raw YDLIDAR payloads and approved S3 gateway captures.
No real S3 capture was available during initial setup, so no binary fixture is
included and no live-network result is claimed.
