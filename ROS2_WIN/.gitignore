# colcon output is kept in Docker named volumes, but ignore local fallback builds.
/build/
/install/
/log/
**/build/
**/install/
**/log/
**/__pycache__/
*.pyc
docker/mapping_console.status
docker/mapping_console.log
# Machine-local ROS Motion Gateway authentication material.
config/private/

# Local ROS bags, maps, evidence, and task planning notes
bags/
evidence/
maps/
.planning/
