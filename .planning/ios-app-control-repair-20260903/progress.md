# Progress

- 2026-09-03: Correct target confirmed from user screenshot. Located complete `IOS-APP` snapshot at `bde0fc6`; restore and inspection are next.
- 2026-09-03: Restored `IOS-APP` source, resources, and `project.pbxproj` from `bde0fc6`. Found its required `Shared/SmartCarAppCore` dependency is also deleted on the current branch; restoring the matching snapshot next.
- 2026-09-03: First shared-package restore command failed before execution because of an unterminated shell quote. No shared files were written.
- 2026-09-03: Restored the matching `Shared/SmartCarAppCore` snapshot. `xcodebuild -list -project SmartCarIOS.xcodeproj` found the `SmartCarIOS` scheme and `swift build` passed.
- 2026-09-03: Updated only `IOS-APP/Sources/SmartCarIOS/Core/BLE/BLEManager.swift`: connected state now requires FFE1/FFE2/FFE3 discovery plus FFE2 Notify confirmation; outgoing controls use direct V1 and one Write With Response in flight; disconnect waits for a four-wheel-zero 0x15 write response, with a one-second bounded timeout. Existing 0x2D/0x2E payload paths are retained.
- 2026-09-03: Reverted the prior accidental `IOS_APP/SmartCarApp` and package changes, and removed its task-created test. `swift build`, `swift test` (14 tests), `xcodebuild -list`, and iPhone Simulator `xcodebuild ... build` passed. BLE delivery and vehicle behavior still require a physical iPhone and SmartCar_S3 session.
- 2026-09-03: User identified the missing iPhone circular joystick. Continue only in `IOS-APP`; inspect the current joystick implementation, correct any control-state edge cases, then rerun SwiftPM, shared-package tests, and iPhone Simulator build. Physical iPhone/BLE/vehicle acceptance remains pending.
- 2026-09-03: The first follow-up patch did not apply because it incorrectly combined a UI hunk and a view-model hunk in one file update. `apply_patch` made no partial source change; split the edits by file before retrying.
- 2026-09-03: Updated only `IOS-APP/Sources/SmartCarIOS/UI/ControlDashboardView.swift` and `IOS-APP/Sources/SmartCarIOS/Core/ViewModels/SmartCarViewModel.swift`. The joystick now always returns visually to center on release; a manual master-scale edit clears the active joystick intent. No BLE/S3/STM32/macOS files were touched.
- 2026-09-03: Validation passed: `IOS-APP` `swift build`; `Shared/SmartCarAppCore` `swift test` (14/14); and `xcodebuild -project SmartCarIOS.xcodeproj -scheme SmartCarIOS -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' build`. These are host/simulator results only; physical iPhone BLE delivery, S3 admission, and vehicle stop behavior remain unverified.
- 2026-09-03: New report: iPhone commands produce no vehicle reaction and the iPhone log feature should be removed for speed. The supplied logs show healthy STM32-S3 traffic and zero wheel targets but no App FFE1/parser/ACK trace. Inspect the live source chain before changing control behavior; remove only the iPhone log feature while preserving FFE2 readiness, telemetry, and stop safety.
- 2026-09-03: Source trace found a mobile-side control gate: `BLEManager` requires FFE3 before publishing connected, but FFE3 is log-only. Thus absent/undiscovered FFE3 blocks `updateMotionIntent` before FFE1. Current S3 command-bridge source registers the BLE receive callback and logs `APP_BLE_RX/TX` every second; their absence in the supplied captures means the flashed S3 image is not source-matched. Next edit: decouple FFE3 entirely, delete its UI/parser/store path, retain FFE1/FFE2 control and telemetry, then rebuild.
- 2026-09-03: Removed iPhone-only FFE3 logging end to end: FFE3 no longer participates in discovery, subscription, readiness, receive parsing, buffering, or UI; removed the log tab and its three Swift source files/project references. Retained FFE1 writes, FFE2 notification readiness/telemetry, command scheduling, stop-before-disconnect, background stop, and all firmware source unchanged. Build/test verification next.
- 2026-09-03: Final host validation passed: `IOS-APP` `swift build`; `Shared/SmartCarAppCore` `swift test` (14/14); `xcodebuild` for generic iOS Simulator; and unsigned generic iPhoneOS `xcodebuild` (`CODE_SIGNING_ALLOWED=NO`). `Resources/Info.plist` passes `plutil -lint`; the iPhone source/project has no FFE3, log view, or log store reference. `iPhoneWang` is offline and no ESP32-S3 USB/JTAG serial port is present, so installation, S3 image binding, BLE command admission, and controlled vehicle stop acceptance remain unverified.
