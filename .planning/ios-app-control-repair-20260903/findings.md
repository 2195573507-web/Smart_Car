# Findings

- User-confirmed target: `/Users/zhiqin/Projects/Smart_Car/IOS-APP` for iPhone.
- The visible `SmartCarIOS.xcodeproj` lacks `project.pbxproj`, which is why Xcode cannot open it.
- `bde0fc6` contains the complete tracked target, including `project.pbxproj`, Swift sources, resources, and package manifest.
- The preceding repair was applied to retired `IOS_APP/SmartCarApp`; those task-created changes must not be part of the result.
- `IOS-APP` previously set its UI state to connected in `didConnect`, before FFE1/FFE2/FFE3 discovery and before FFE2 Notify CCC completion. It also queued control while still connecting, negotiated V2 immediately, and used Write Without Response for 0x2E.
- The current S3 source accepts both App BLE V1 and V2. The iPhone repair deliberately keeps the existing V1 command wire format, matching the macOS reference; it preserves V1 0x15 wheel, 0x2D chassis-speed, and 0x2E heading commands without a firmware change.
- The two supplied device logs only show one prior `BLE_PREV_DISC reason=0x13`; they contain no raw FFE1 command frame or command ACK, so they cannot establish the device-side cause of the disconnect.
- The iPhone control dashboard lacked the macOS-style circular joystick. The intended implementation must keep the established `[RR, RF, LR, LF]` wheel order and send only the existing App BLE V1 `0x15` wheel-speed command; releasing the control must reuse the existing zero-wheel safety path.
- The two supplied logs show STM32-S3 SRP synchronization and repeated MotorBoard `MTEP:0,0,0,0`, but do not record the iPhone's FFE1 write, S3 App-BLE frame admission, or command ACK. They establish that no nonzero command reached the MotorBoard during the capture, not where the App-to-vehicle chain failed.
- User requests complete removal of the iPhone in-app log feature and an App speed optimization. The intended scope is the iPhone log UI and its FFE3 parser/store/publication work; FFE2 control-readiness, telemetry, disconnect safety, and the critical command path remain in scope and must be preserved.
- Current iPhone `BLEManager` incorrectly includes `loggerCharacteristic` (FFE3) in both `hasRequiredCharacteristics` and `completeGATTSetupIfReady`. A missing/undiscovered FFE3 therefore keeps the App out of `connected`, and `updateMotionIntent` rejects every joystick command before any FFE1 write. FFE3 is a logging-only path and must not gate control readiness.
- The current S3 source registers the App BLE receive callback in `smartcar_service_init()` and emits `APP_BLE_RX`/`APP_BLE_TX` diagnostics each second. Neither supplied device log contains those markers despite `BLE_CONNECTED`; this is evidence that the tested S3 image is not proven to match the current command-bridge source, not evidence that a mobile-only edit can make the vehicle move.
- Removing the iPhone log feature can reduce live work by removing FFE3 discovery/subscription, `BLELogReceivePipeline`, log stores, and the log navigation screen. Keep FFE2 parsing because it drives telemetry, vehicle-state updates, GATT readiness, and safety feedback.
- Revalidation on 2026-09-03 passed: `IOS-APP` SwiftPM build, `Shared/SmartCarAppCore` 14/14 tests, iOS Simulator build, and unsigned generic iPhoneOS build. `Info.plist` also passes `plutil -lint`; source and project metadata have no FFE3/log-view/store references outside ignored historical build products.
- `iPhoneWang` is currently offline and this Mac exposes no ESP32-S3 USB/JTAG serial port. No new App could be installed, no S3 firmware could be flashed, and no end-to-end command evidence was collected in this task.
